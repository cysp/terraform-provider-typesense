---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "{{.Name}} {{.Type}} - {{.ProviderName}}"
subcategory: ""
description: |-
{{ .Description | plainmarkdown | trimspace | prefixlines "  " }}
---

# {{.Name}} ({{.Type}})

{{ .Description | trimspace }}

{{ if .HasExample -}}
## Example Usage

{{tffile .ExampleFile }}
{{- end }}

{{ if eq .Name "typesense_collection" -}}
## Timeouts

Collection updates default to thirty minutes. Create, read and delete operations default to five minutes. Set `timeouts` to override these limits.
{{ else if or (eq .Name "typesense_alias") (eq .Name "typesense_key") -}}
## Timeouts

Operations default to five minutes. Set `timeouts` to override these limits. Changing a timeout does not change the Typesense object.
{{ end }}
{{ if eq .Name "typesense_key" -}}
See [API key lifecycle](../guides/keys) for permissions, expiration, rotation and secret handling.

{{ end -}}
{{ .SchemaMarkdown | trimspace }}

{{ if .HasImport -}}
## Import

Import is supported using the following syntax:

{{codefile "shell" .ImportFile }}

See [Importing existing resources](../guides/importing) for import blocks using resource identities.
{{- end }}
